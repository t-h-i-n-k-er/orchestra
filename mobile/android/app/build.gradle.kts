plugins {
    id("com.android.application")
}

android {
    namespace = "com.orchestra"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.orchestra"
        minSdk = 26
        targetSdk = 34
        versionCode = 1
        versionName = "1.0.0"
    }

    buildTypes {
        release {
            isMinifyEnabled = true
            isShrinkResources = true
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
        debug {
            isDebuggable = true
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    sourceSets {
        getByName("main") {
            jniLibs.srcDirs("src/main/jniLibs")
        }
    }
}

dependencies {
    // No external dependencies needed — the agent is self-contained.
    // The Rust shared library (liborchestra.so) is loaded from jniLibs/.
}