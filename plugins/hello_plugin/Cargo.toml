[package]
name = "hello_plugin"
description = "Example plugin demonstrating the Orchestra agent plugin interface"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
module_loader = { path = "../../module_loader" }
anyhow = "1.0"
