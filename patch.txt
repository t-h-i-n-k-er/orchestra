--- builder/src/main.rs
+++ builder/src/main.rs
@@ -91,5 +91,8 @@
 
-            let encrypted_bytes =
-                common::crypto::encrypt_aes_gcm(&final_agent_bytes, &enc_key, &hmac_key)?;
+            let encrypted_bytes = {
+                let session = common::CryptoSession::from_shared_secret(&enc_key);
+                session.encrypt(&final_agent_bytes)
+            };
 
             let dist_dir = std::path::PathBuf::from("dist");
